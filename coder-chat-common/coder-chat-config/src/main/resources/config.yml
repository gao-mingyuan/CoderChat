spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${coderChat.nacos.server.url}
      config:
        server-addr: ${coderChat.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}